name: Daily productivity check

on:
  schedule:
    - cron: "50 22 * * *" # daily at 22:50 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  check-commits:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Get today's date (UTC)
        id: date
        run: |
          TODAY=$(date -u +%Y-%m-%d)
          echo "TODAY=$TODAY" >> $GITHUB_ENV

      - name: Count commits today
        id: count
        run: |
          git fetch --all
          COUNT=$(git rev-list --all --count --since="${TODAY}T00:00:00Z" --until="${TODAY}T23:59:59Z" || true)
          echo "commits=$COUNT" >> $GITHUB_OUTPUT
          echo "COUNT=$COUNT" >> $GITHUB_ENV

      - name: Create reminder commit if no commits today
        if: steps.count.outputs.commits == '0'
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          REPO: ${{ github.repository }}
          USER_PAT: ${{ secrets.USER_PAT }}
          USER_NAME: ${{ secrets.COMMIT_USER_NAME }}
          USER_EMAIL: ${{ secrets.COMMIT_USER_EMAIL }}
        run: |
          if [ -z "$USER_PAT" ]; then
            echo "Missing secret USER_PAT. Add a personal access token (repo scope) as secret USER_PAT." >&2
            exit 1
          fi
          if [ -z "$USER_NAME" ] || [ -z "$USER_EMAIL" ]; then
            echo "Missing COMMIT_USER_NAME or COMMIT_USER_EMAIL secrets. Add your name and email." >&2
            exit 1
          fi
          # FILE="productivity-reminder-${TODAY}.md"
          # echo "Hey, you have not been productive today, Suray." > "$FILE"
          git config user.name "$USER_NAME"
          git config user.email "$USER_EMAIL"
          git add "$FILE"
          git commit -m "chore: reminder — no commits on ${TODAY}" --author="$USER_NAME <$USER_EMAIL>"
          git push "https://$USER_PAT@github.com/$REPO" HEAD:"$DEFAULT_BRANCH"
